# Leave unset to run standalone from frontend/fixtures/ (default).
# Set to a deployed API base (e.g. https://xyz.execute-api.ap-south-1.amazonaws.com/v1)
# to point the app at the real backend instead.
VITE_API_URL=
